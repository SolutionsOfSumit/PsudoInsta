[project]
name = "fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sumit", email = "officially.sumit.upadhyay@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
fastapi = "fastapi:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
